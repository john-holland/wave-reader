var StartMessage = /** @class */ (function () {
    function StartMessage(attributes) {
        if (attributes === void 0) { attributes = {}; }
        this.name = 'start';
        Object.assign(this, attributes);
    }
    return StartMessage;
}());
export default StartMessage;
